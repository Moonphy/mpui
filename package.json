{
  "name": "mpui",
  "private": false,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "install": "lerna bootstrap",
    "build": "lerna run build",
    "clean": "lerna clean",
    "change": "lerna changed",
    "publish": "lerna publish",
    "release": "lerna publish --loglevel 5",
    "release-ci": "lerna publish from-package --no-git-reset --yes --loglevel 5"
  },
  "devDependencies": {
    "lerna": "^6.1.0"
  }
}